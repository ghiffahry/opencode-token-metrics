# Token Metrics - Python runtime dependencies
# One-time install: py -m pip install -r requirements.txt
#
# pywebview        -> desktop app (app/desktop.py)
# Pillow           -> icon generation (tools/make_icon.py)
#
# Optional (build the desktop exe):
#   pyinstaller    -> tools/build_desktop.py --build
pywebview>=4.0
Pillow>=9.0
